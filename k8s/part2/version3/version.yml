apiVersion: apps/v1
kind: Deployment
metadata:
  name: version-backend-deployment
  labels:
    app: version-backend
spec:
  replicas: 3
  template:
    spec:
      containers:
          image: coderpews/version-app:3.0